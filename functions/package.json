{
  "name": "whatthetetris-functions",
  "description": "Serverless functions backing docs/TECHNICAL_ARCHITECTURE.md SS4 (score validation) and docs/MONETIZATION.md (RevenueCat entitlement sync). Deployed to Firebase Cloud Functions — nothing here runs on a server anyone hosts or patches.",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
